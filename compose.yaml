services:
  api:
    build:
      context: .
      dockerfile: server/Dockerfile

    container_name: flowdesk-api-container

    environment:
      MONGOMS_VERSION: "7.0.14"
      MONGOMS_DISTRO: "ubuntu-22.04"
      BULLMQ_REDIS_URL: redis://redis:6379

    depends_on:
      - mongodb
      - redis

    env_file:
      - server/.env

    ports:
      - "3001:3001"

    command: npm run dev

    volumes:
      - ./server:/app/server
      - ./shared:/app/shared
      - /app/server/node_modules

  notification-worker:
    build:
      context: .
      dockerfile: server/Dockerfile

    container_name: flowdesk-notification-worker-container

    environment:
      BULLMQ_REDIS_URL: redis://redis:6379

    depends_on:
      - mongodb
      - redis

    env_file:
      - server/.env

    command: npm run worker:notifications:dev

    volumes:
      - ./server:/app/server
      - ./shared:/app/shared
      - /app/server/node_modules

  client:
    build:
      context: .
      dockerfile: client/Dockerfile

    container_name: flowdesk-client-container

    ports:
      - "5173:5173"

    command: npm run dev -- --host 0.0.0.0

    volumes:
      - ./client:/app/client
      - ./shared:/app/shared
      - /app/client/node_modules

  mongodb:
    image: mongo:8
    container_name: flowdesk-mongodb-container

    command: ["mongod", "--replSet", "rs0", "--bind_ip_all"]

    ports:
      - "27017:27017"

    volumes:
      - mongodb_data:/data/db

  redis:
    image: redis:7-alpine
    container_name: flowdesk-redis-container
    ports:
      - "6379:6379"

volumes:
  mongodb_data:
