import type { ActivityDto } from "@shared/types/dto/activity/activity.dto"

const ActivityItem = ({activity}: {activity: ActivityDto}) => {
    return(
        <div>
            ActivityItem
        </div>
    )
}
export default ActivityItem